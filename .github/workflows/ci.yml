name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      # CI validation note: for any package-manager question, run or inspect
      # every script pathway first and report them all before deciding whether
      # pnpm is required.
      - run: echo "CI placeholder"
